
# - этот знак означает комментарий, все что пишется после него на этой строке никак не обрабатывается
# Имя файла это имя контента по умолчанию.
# Расширение файла отвечает за то какой объект создается. По умолчанию создается 1 объект на 1 файл. Чтобы создавать 2 и более объектов нужно использовать действия new и copy
# Записи делаются в трех вариациях:

%Action %Value
%Key = %Value
%Key:%Info = %Value

# %Action это действие
# %Value это данные 
# %Key это ключ, значение которое меняется
# %Info дополнительная информация для ключа

# действия, актуальны для всех типов объектов.
new NewContentName # После этой строки старый объект отправится в библиотеку объектов, и создастся новый объект с именем NewContentName, все дальнейшие ключи будут относиться к новому объекту
copy CopyNewContentName # аналогично предыдущему действию, но новый объект будет содержать все те же данные что и старый, кроме имени

# Ключи контента

Name = NewName # после этой строки имя сменится на NewName

LocationPid = %pid # задает идентификатор прототипа локации для контента. Идентификатор прототипа ( пид ) вводится в единичном экземпляре, числом
	# Пример:
LocationPid = 1 # Пид локации будет 1

Size = %х %у # Задает размер локации на глобальной карте для осей "х" и "у"
	# Пример:
Size = 1 10 # Локация на глобальной карте будет занимать 1 клетку в ширину, и 10 клеток в высоту

Flags = %value 	# Перечисление флагов локации, может быть несколько флагов одновременно, вводить через отступы ( пробел, табуляция )
				# Возможные значения:
					# Encounter - локация это энкаунтер
					# Static - локация статична, видна с глобала и не удаляется
					# Ignored - мир игнорирует существование этой локации, необходимо для карт регистрации, обучения и подобного.
		# Пример:
Flags = Static Encounter # Локация будет статичным энкаунтером
				
Visible = %value	# Видимость локации на глобале, чем выше значение, тем легче увидеть эту локацию. Значение от -128 до 127
	
Create:%SubName = %x %y # Создает экземпляр этой локации с внутренним именем SubName в позиции x y
				#Пример:
Create:Phoenix = 25 555 # Создаст карту присвоив ей внутренне имя Phoenix в координатах на глобале 25 555

			# !!!! Внутренее имя в данный момент ничего не значит, но старайтесь называть корректно и без повторений. Это нужно в дальнейшем для дебага и прочего.
